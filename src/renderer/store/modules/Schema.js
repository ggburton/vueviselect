const state = {
  schema: 'fish'
}

const mutations = {
  setSchema (state, payload) {
    state.schema = payload.schema
  }
}

const actions = {

}

export default {
  state,
  mutations,
  actions
}
